Page opens
    ↓
getAdvice()
    ↓
Disable button
    ↓
fetch()
    ↓
Receive response
    ↓
Convert JSON
    ↓
Update HTML
    ↓
Enable button

User clicks button
    ↓
Repeat
Semantic HTML structure.
Responsive layouts using CSS and media queries.
Flexbox for alignment and centering.
Custom styling based on the Frontend Mentor design specifications.
Hover effects and rounded card layouts.
DOM element selection using querySelector().
JavaScript functions to organize code.
Event listeners to respond to user interactions.
Fetch API to retrieve data from an external source.
Error handling for failed requests.
Dynamic updating of page content using textContent.
Template literals for displaying advice numbers and messages.
